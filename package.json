{
  "name": "pharmamedian-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "PharmMedian — Closed-Loop Drug Return, Destruction & Audit Platform",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
